# راهنمای توضیحات جداول اسکیمای مرجع (Legacy Reference Schema — Table Guide)

> این فایل توضیح می‌دهد هر جدول در اسکیمای مرجع (`LEGACY_SCHEMA.sql`) چه هدفی داشته و مفهوماً با کدام جداول دیگر در ارتباط بوده است — حتی جاهایی که آن رابطه به‌صورت Foreign Key واقعی پیاده‌سازی نشده. این سند صرفاً برای فهم منطق کسب‌وکار سیستم قدیمی است؛ طراحی جدید (Code-First) باید بر اساس بخش ۴ و ۹ پرامپت پروژه ساخته شود، نه با کپی مستقیم این ساختار.

| نام جدول | توضیح | جداول مرتبط |
|---|---|---|
| API | فهرست Endpointها و عملیات API سامانه را شامل نام کنترلر، نشانی مسیر و نوع عملیات HTTP نگهداری می‌کند. این جدول مبنای تعریف دسترسی نقش‌ها و اتصال عملیات فرایندی به API است. | Permissions، Process |
| AuthAccessTokenBlacklists | شناسه Access Tokenهای باطل‌شده را تا زمان انقضای آن‌ها نگهداری می‌کند تا توکن‌هایی که پیش از پایان اعتبار لغو شده‌اند، دوباره قابل استفاده نباشند. | از نظر مفهومی: AuthSessions، AuthRefreshTokens، Users |
| AuthLoginLockouts | تعداد تلاش‌های ناموفق ورود هر نام کاربری، زمان آخرین تلاش، IP و مدت مسدودبودن ورود را نگهداری می‌کند. برای مقابله با حملات حدس رمز عبور و ورودهای مکرر ناموفق استفاده می‌شود. | از طریق نام کاربری و به‌صورت مفهومی با Users مرتبط است. |
| AuthRefreshTokens | Refresh Tokenهای کاربران را به‌صورت Hash‌شده ذخیره می‌کند و اطلاعات چرخه عمر، مصرف، ابطال، جایگزینی و زنجیره توکن‌ها را نگه می‌دارد. این جدول امکان Token Rotation، تشخیص استفاده مجدد و مدیریت نشست‌ها را فراهم می‌کند. | Users، AuthSessions |
| AuthSessions | نشست‌های ورود کاربران را همراه با شناسه نشست، زمان ایجاد و آخرین فعالیت، IP، مرورگر یا دستگاه و وضعیت ابطال نگهداری می‌کند. هر کاربر می‌تواند چند نشست فعال داشته باشد. | Users، AuthRefreshTokens |
| DocContent | محتوای متنی منابع دیجیتال را در سطح جلد، بخش، صفحه و بند ذخیره می‌کند. هر رکورد به یک موجودیت دانشی تعریف‌شده در TMD متصل است و می‌تواند نماینده بخشی از یک کتاب، مقاله یا سند دیجیتال باشد. | TMD |
| Domains | دامنه‌ها و حوزه‌های موضوعی مانند فقه، کلام و مدیریت اطلاعات را به‌صورت سلسله‌مراتبی نگهداری می‌کند. هر دامنه می‌تواند زیرمجموعه دامنه‌ای دیگر باشد و وضعیت مشخصی داشته باشد. | Domains (رابطه والد و فرزند)، States، TMD، Themes |
| Files | اطلاعات و نشانی تمام فایل‌های مورد استفاده سامانه، مانند تصویر خبر، فایل سند، مقاله، لوگوی سازمان و پیوست‌ها را نگهداری می‌کند. ارتباط فایل با رکورد مقصد به‌صورت عمومی و چندریختی، از طریق نام جدول و شناسه رکورد، تعریف شده است. | به‌صورت مفهومی با تمام جداول دارای فایل، از جمله News، Organizations، Projects، TMD و DocContent مرتبط است؛ Foreign Key مستقیم ندارد. |
| Logs | رویدادها و فعالیت‌های انجام‌شده روی رکوردهای سامانه را برای ثبت سابقه و ممیزی نگهداری می‌کند. هر Log شامل جدول و رکورد مقصد، نوع عملیات، شرح فعالیت، کاربر و زمان وقوع است. | LogType، Users؛ به‌صورت چندریختی با سایر جداول سامانه |
| LogType | انواع رویدادهای قابل ثبت در سیستم، مانند ایجاد، ویرایش، حذف، ورود، خروج یا تغییر وضعیت را تعریف می‌کند. | Logs |
| Modules | ماژول‌های پویا و سلسله‌مراتبی سامانه، مانند اصطلاح‌نامه، هستی‌شناسی، گراف دانش، دانشنامه، کتابخانه و نمایه‌سازی را تعریف می‌کند. هر ماژول می‌تواند زیرماژول داشته باشد و مبنای سازمان‌دهی قابلیت‌ها و موجودیت‌های دانشی باشد. | Modules (رابطه والد و فرزند)، MRT، TMD، Themes |
| MRT | جدول واسط «ماژول ـ نوع رابطه» است و تعیین می‌کند در هر ماژول چه انواع رابطه‌ای مجاز یا قابل استفاده هستند؛ برای مثال رابطه سلسله‌مراتبی در اصطلاح‌نامه یا رابطه نویسندگی در کتابخانه. | Modules، RelationsTypes، RelationConstraints |
| News | خبرها و محتوای اطلاع‌رسانی سامانه را شامل عنوان، خلاصه، متن کامل و پیوند خارجی نگهداری می‌کند. | NewsRelNewsCategory، NewsRelTags؛ به‌صورت مفهومی Files |
| NewsCategory | دسته‌بندی‌های خبر متعلق به هر سازمان را همراه با عنوان، توضیح و تصویر نگهداری می‌کند. | Organizations، NewsRelNewsCategory؛ به‌صورت مفهومی Files |
| NewsRelNewsCategory | جدول واسط میان خبرها و دسته‌بندی‌های خبری است و امکان انتساب هر خبر به یک یا چند دسته و هر دسته به چند خبر را فراهم می‌کند. | News، NewsCategory |
| NewsRelTags | جدول واسط میان خبرها و اصطلاحات مورد استفاده به‌عنوان برچسب است. هر خبر می‌تواند چند برچسب داشته باشد و هر اصطلاح نیز برای چند خبر استفاده شود. | News، Terms |
| Organizations | سازمان‌ها، واحدها و مجموعه‌های استفاده‌کننده از سامانه را به‌صورت سلسله‌مراتبی تعریف می‌کند. اطلاعات تماس و ساختار والد و فرزند سازمان‌ها در این جدول قرار دارد و هر سازمان می‌تواند کاربران، نمایندگان، انواع پروژه، خبرها و تنظیمات اختصاصی داشته باشد. | Organizations (رابطه والد و فرزند)، OrganizationUsers، ProjectType، NewsCategory، Themes |
| OrganizationUsers | جدول عضویت کاربران در سازمان‌ها است. مشخص می‌کند هر کاربر عضو کدام سازمان است و آیا به‌عنوان نماینده یا مسئول مدیریتی آن سازمان فعالیت می‌کند. | Organizations، Users، Payments |
| Payments | مبالغ یا پرداخت‌های مرتبط با یک پروژه و یکی از اعضای سازمان را نگهداری می‌کند. این جدول در ساختار فعلی مبلغ را ثبت می‌کند، اما اطلاعاتی مانند تاریخ، وضعیت، روش پرداخت و شماره سند در آن وجود ندارد. | OrganizationUsers، Projects |
| Permissions | مجوز دسترسی نقش‌ها به Endpointها و عملیات API را تعریف می‌کند. هر رکورد مشخص می‌کند یک نقش نسبت به یک API چه سطح یا مقدار دسترسی دارد. | Roles، API |
| Process | مراحل و قواعد اجرایی فرایند مربوط به هر نوع پروژه را تعریف می‌کند. هر رکورد نماینده یک مرحله یا گام فرایندی با وضعیت، نقش مسئول، عملیات API، مرحله قبلی و بعدی، مدت انجام و شرط انتقال است. | ProjectType، States، Roles، API، Projects؛ همچنین به‌صورت مفهومی با خود Process از طریق Process_Next و Process_Previous |
| Projects | نمونه‌های اجرایی فعالیت‌ها یا پروژه‌های سامانه را نگهداری می‌کند. هر پروژه می‌تواند از یک نوع پروژه پیروی کند، در یکی از مراحل فرایند قرار داشته باشد و دارای تاریخ شروع، پایان و سابقه متنی مرحله قبلی باشد. | ProjectType، Process، ProjectUsers، Payments، TMDP؛ به‌صورت مفهومی Files و Logs |
| ProjectType | انواع قابل تعریف پروژه یا فعالیت را برای هر سازمان نگهداری می‌کند. نوع پروژه مبنای تعریف فرایند، مراحل و نمونه‌های اجرایی پروژه است. | Organizations، Process، Projects |
| ProjectUsers | جدول واسط میان کاربران و پروژه‌ها است و اعضا، مشارکت‌کنندگان یا کاربران تخصیص‌یافته به هر پروژه را مشخص می‌کند. | Users، Projects |
| RelationConstraints | محدودیت‌ها و قواعد اعتبارسنجی مربوط به یک نوع رابطه در یک ماژول را تعریف می‌کند. این قواعد می‌توانند شامل کاردینالیتی دوطرفه، طول مجاز، الگوی اعتبارسنجی، شدت خطا و پیام خطا باشند. ستون SqlCondition برای نگهداری شرط‌های قدیمی در نظر گرفته شده، اما اجرای مستقیم محتوای آن به‌عنوان SQL می‌تواند خطر امنیتی داشته باشد. | MRT و از طریق آن Modules و RelationsTypes |
| Relations | نمونه‌های واقعی رابطه میان دو موجودیت TMD را نگهداری می‌کند. نوع، اولویت، سطح، توضیح و مکان‌نما یا شناسه موضع رابطه نیز در این جدول ثبت می‌شود؛ مانند رابطه اعم و اخص، نویسندگی یا وابستگی مفهومی. | TMD در نقش مبدأ و مقصد، RelationsTypes |
| RelationsTypes | انواع روابط، ویژگی‌ها و فیلدهای سفارشی قابل استفاده در ساختار پویای داده‌های سامانه را تعریف می‌کند. هر رکورد می‌تواند نمایانگر یک رابطه میان دو موجودیت TMD، مانند رابطه «نویسندگی» میان یک TMD از نوع کتاب و یک TMD از نوع اَعلام، یا نمایانگر یک ویژگی و فیلد سفارشی برای موجودیت‌ها باشد. این جدول عنوان رابطه، عنوان معکوس، نوع داده فیلد، ساختار والد و فرزند و ویژگی‌های منطقی رابطه مانند سلسله‌مراتبی‌بودن، تعدی‌پذیری، نامتقارن‌بودن، هم‌ارزی و بازتاب‌ناپذیری را نگهداری می‌کند. همچنین مشخص می‌کند نوع رابطه یا فیلد توسط سیستم تعریف شده یا به‌صورت سفارشی ایجاد شده و آیا در رابط کاربری به‌عنوان برچسب گره نمایش داده می‌شود. امکان استفاده از هر نوع رابطه در ماژول‌های مختلف از طریق جدول MRT و قواعد اعتبارسنجی، نوع مبدأ و مقصد و کاردینالیتی آن از طریق RelationConstraints مدیریت می‌شود. | RelationsTypes در نقش والد، فرزند یا نوع داده، MRT، Relations |
| Roles | نقش‌های سامانه مانند مدیر سیستم، مدیر سازمان، کارشناس یا ناظر را تعریف می‌کند. نقش‌ها از طریق مجوزها به APIها دسترسی پیدا می‌کنند و می‌توانند مسئول اجرای مراحل فرایند باشند. | UserRoles، Permissions، Process |
| States | فهرست عمومی وضعیت‌های قابل استفاده در بخش‌های مختلف سامانه را نگهداری می‌کند؛ مانند پیش‌نویس، فعال، تأییدشده، ردشده، در حال انجام یا پایان‌یافته. | Domains، Terms، Process، TMD، TMDP |
| Terms | اطلاعات پایه اصطلاحات، مفاهیم، عناوین، نام‌ها یا مدخل‌های دانشی را نگهداری می‌کند. هر اصطلاح دارای عنوان، توضیح و وضعیت است و می‌تواند در ماژول‌ها و دامنه‌های مختلف به کار رود. | States، TMD، NewsRelTags |
| TMD | جدول محوری «اصطلاح ـ ماژول ـ دامنه» است و مشخص می‌کند یک اصطلاح در کدام ماژول و حوزه موضوعی به کار رفته است. این جدول نمایش زمینه‌مند هر اصطلاح را ایجاد می‌کند و وضعیت و ترجیحی‌بودن آن را نگهداری می‌کند؛ بنابراین یک اصطلاح می‌تواند در چند ماژول یا دامنه، کاربردهای متفاوتی داشته باشد. | Terms، Modules، Domains، States، Relations، DocContent، TMDP |
| TMDP | جدول واسط میان موجودیت‌های زمینه‌مند TMD و پروژه‌ها است. مشخص می‌کند یک مفهوم، اصطلاح یا منبع دانشی در چه پروژه‌ای، با چه وضعیت و در چه بازه زمانی استفاده یا تولید شده است. | TMD، Projects، States |
| UserRoles | جدول واسط میان کاربران و نقش‌ها است و امکان اختصاص یک یا چند نقش به هر کاربر و انتساب هر نقش به چند کاربر را فراهم می‌کند. | Users، Roles |
| Users | اطلاعات حساب کاربری و مشخصات فردی کاربران سامانه را نگهداری می‌کند؛ از جمله نام کاربری، رمز عبور ذخیره‌شده، نام، نام خانوادگی، اطلاعات تماس و زمان ثبت‌نام و آخرین ورود. مقدار User_Password باید صرفاً Hash امن رمز عبور باشد و نباید رمز خام در آن ذخیره شود. | UserRoles، OrganizationUsers، ProjectUsers، Logs، AuthSessions، AuthRefreshTokens؛ به‌صورت مفهومی AuthLoginLockouts |
| Themes | رابط‌های کاربری پویا و قابل پیکربندی سامانه را ذخیره می‌کند. هر Theme شامل نام و کد کامل رابط کاربری یک صفحه است و می‌تواند برای یک سازمان، ماژول و در صورت نیاز یک دامنه موضوعی خاص تعریف شود. ساختار و اجزای این رابط کاربری در سامانه قابل تعریف و سفارشی‌سازی است. نوع و کاربرد صفحه نیز از طریق ThemeTypes مشخص می‌شود. | Organizations، Modules، Domains، ThemeTypes |
| ThemeTypes | انواع رابط‌های کاربری قابل تعریف در سامانه را نگهداری می‌کند. این جدول کاربرد و جایگاه هر Theme را مشخص می‌کند؛ مانند صفحه اصلی، صفحه نتایج جست‌وجو، صفحه نمایش تک‌محتوا، صفحه فهرست، صفحه جزئیات یا سایر صفحات قابل طراحی. | Themes |
